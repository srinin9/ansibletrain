---
-
  hosts: webserver
  gather_facts: False

  vars:
    motd: "Welcome to webserver server - Ansible Rocks - modified"
  tasks:
    - name: configure a mot
      copy:
        content: "{{ motd }}"
        dest: /etc/motd
  handlers:
    - name: MOTD changed
      debug:
        msg: Thsi MOTD was changed 
...